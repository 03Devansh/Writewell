# Clear Auth State

If you're stuck in a loading state, you can clear the auth state by:

1. Open browser console (F12)
2. Run: `localStorage.removeItem('inkwell_token')`
3. Refresh the page

This will clear any stuck authentication state.
